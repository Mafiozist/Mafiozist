# WORKLOG — журнал прогонов агентов

> **Что это за файл.** Долговечный (в git) журнал мульти-агентных прогонов, сгенерировавших документацию/код DEVNOTES. Нужен, чтобы **после конца сессии** подтянуть контекст: что делали, каким процессом, что осталось. Транскрипты воркфлоу живут в эфемерном контейнере и теряются — durability обеспечивается **только коммитом в git**. Дополняется (append); свод статуса — в журнале [`../CLAUDE.md`](../CLAUDE.md) §7.

## Связанные документы

- [`00-INDEX.md`](00-INDEX.md) — оглавление · [`../CLAUDE.md`](../CLAUDE.md) — состояние и конвенции (§3.8 — журналирование).

---

## Прогон #1 — 2026-07-17 · Комплект проектной документации

**Девиз проекта (из WBS fable):** DEVNOTES — local-first терминал инженерных знаний: заметки по технологиям, мгновенный FTS5-поиск, синхронизация с Яндекс.Диском.

**Процесс (роли):** `fable` — постановка задач (единый WBS: фичи MoSCoW, модель данных, риски, согласованные термины) и критика каждого документа; `opus` — реализация документов; доработка по замечаниям критика. Пайплайн: Постановка → Реализация → Критика → Доработка → Кросс-документация → Журнал.

**Источник концепций:** веб-проект `Mafiozist/Portfolio` (.NET + React) — перенята иерархия `Project → NoteSeries → NoteContent`, теги, терминально-хакерская дизайн-эстетика и токены shadcn/ui.

**Метрики прогона:** 36 агентов (31 успешно, 5 не выполнено), ~1.58M токенов суб-агентов, ~41 мин.

### Статус документов

| Документ | Критика (fable) | Доработка (opus) | Примечание |
| --- | --- | --- | --- |
| `README.md` | пройдена | — | |
| `CLAUDE.md` | пройдена | доработан вручную | сведены нумерация/кросс-ссылки под факт. файлы; +§3.7 тесты, +§3.8 журнал |
| `docs/01-VISION.md` | пройдена | по замечаниям | |
| `docs/02-SPECIFICATION.md` | пройдена | по замечаниям | большое ТЗ, 917 строк |
| `docs/03-FEATURES.md` | пройдена | по замечаниям | |
| `docs/04-ARCHITECTURE.md` | пройдена | по замечаниям | |
| `docs/05-DATA-MODEL.md` | пройдена | по замечаниям | |
| `docs/06-UI-UX.md` | пройдена | по замечаниям | |
| `docs/07-TECH-STACK.md` | замечания | ⛔ не применено | fix-агент упал по лимиту сессии |
| `docs/08-SEARCH.md` | замечания | ⛔ не применено | fix-агент упал по лимиту сессии |
| `docs/09-YANDEX-DISK.md` | замечания | ⛔ не применено | fix-агент упал по лимиту сессии |
| `docs/10-ROADMAP.md` | замечания | ⛔ не применено | fix-агент упал по лимиту сессии |
| `docs/12-GLOSSARY.md` | пройдена | по замечаниям | |

### Что осталось (TODO следующей сессии)

1. **Применить замечания критика** к 4 документам: `07-TECH-STACK`, `08-SEARCH`, `09-YANDEX-DISK`, `10-ROADMAP` (первичные версии написаны и пригодны, но не прошли фазу доработки — упёрлись в лимит сессии, сброс 4:50am UTC). Замечания можно перезапустить: `Workflow({scriptPath: '…/scratchpad/devnotes-docs.wf.js', resumeFromRunId: 'wf_3ac3cd71-eff'})` — готовые агенты вернутся из кэша, выполнятся только упавшие fix-шаги.
2. Довести до реализации по `10-ROADMAP.md` (MVP): каркас `src-tauri/` (Rust-ядро) и `src/` (React), миграции, FTS5, тесты по `13-TESTING.md`.

### Сделано вручную (main loop) после лимита

- Сведены нумерация и кросс-ссылки в `CLAUDE.md` под фактические имена файлов.
- Созданы `docs/00-INDEX.md`, `docs/11-FILE-INDEX.md`, `docs/13-TESTING.md` (тесты — по запросу пользователя), этот `WORKLOG.md`.
- Всё закоммичено и запушено в ветку `claude/dev-notes-app-design-xa33vz`.

---

## Прогон #2 — 2026-07-17 · Реализация MVP

**Роль opus (реализация в main loop, без суб-агентов — лимит сессии).** Проверка среды: Rust 1.94 ✅, Node 22 ✅, `webkit2gtk` ❌ (GUI Tauri локально не собрать). Решение: ядро вынесено в отдельный крейт `core/` без Tauri-зависимости — компилируется и тестируется локально.

**Сделано и проверено:**

| Компонент | Артефакт | Проверка |
| --- | --- | --- |
| Ядро `core/` (`devnotes-core`) | миграция 001 (схема+FTS5+триггеры), домен, порты, `SqliteStore`, `search`, `sync` (LWW), `NotesService` | `cargo test` — **18/18 зелёные**; `cargo clippy` — без предупреждений; `cargo fmt` применён |
| Оболочка `src-tauri/` (Tauri 2) | IPC-команды поверх ядра, инициализация БД, `tauri.conf.json`, capabilities | код валиден; локально не собирается (нет WebKit) — соберётся в CI/на машине с webkit2gtk |
| Фронтенд `app/` (React 19+Vite) | дизайн-токены, UI-примитивы (CVA), трёхпанельная раскладка, командная палитра, IPC-обёртка с браузерным моком | `tsc --noEmit` + `vite build` — **зелёные**; `vitest` — **5/5 зелёные** |
| CI | `.github/workflows/devnotes-ci.yml` | тесты ядра (fmt/clippy/test) + фронтенда (typecheck/test/build), фильтр путей `DevNotes/**` |

**Уточнение архитектуры:** ядро — отдельный крейт `core/` (не внутри `src-tauri/`); фронтенд — `app/` (не `src/`). Причина: тестируемость ядра без GUI. Отражено в `CLAUDE.md` §2 и `11-FILE-INDEX.md`.

**Не реализовано (следующие шаги, по `10-ROADMAP.md`):** синхронизация с Яндекс.Диском (v0.3), версии/вложения/wiki-links/backlinks (v1.0), граф/spaced-repetition/шифрование/mobile (v2.0); иконки `src-tauri/icons/*`; e2e Playwright; доработка 4 документов по замечаниям критика (см. Прогон #1).

---

## Прогон #3 — 2026-07-17 · Механизм тегов технологий

**Роль opus (main loop).** По запросу пользователя добавлены теги для поиска по проектам/технологиям (вместо кодирования в названии), как в Portfolio.

**Сделано и проверено:**

| Слой | Что | Проверка |
| --- | --- | --- |
| Ядро `core/` | `TechTag`/`TechTagType`; методы create/list/delete тегов и категорий, `list_tags_for_series`, `set_series_tags` (replace-семантика); `search(raw, tag_ids, limit)` с фильтром по тегам (AND — серия должна иметь ВСЕ выбранные теги; поиск только по тегам без текста) | `cargo test` **22/22**, clippy чисто |
| Оболочка `src-tauri/` | 7 IPC-команд тегов + обновлённая команда `search` (принимает `tagIds`) | код валиден |
| Фронтенд `app/` | `TagSelector` (дропдаун: поиск, чипы-переключатели, создание тега, «сбросить»), `SeriesTagsBar` (теги серии над блоками), фильтр по технологиям в командной палитре поиска | `tsc`+`vite build` ✅, `vitest` **8/8** |

**UX:** выбрать технологии в палитре → поиск сузится до серий с этими тегами; можно искать и без текста — только по выбранным тегам. Теги назначаются серии кнопкой «Изменить теги».

---

## Прогон #4 — 2026-07-17 · Доводка редактора и фундамент синхронизации

**Роль opus (main loop).** Итеративные инкременты поверх MVP:

| Инкремент | Суть | Проверка |
| --- | --- | --- |
| Редактор блоков | Редактирование на месте (`update_content`) + drag-and-drop сортировка (`@dnd-kit`, `reorder_content`) | сборка/тесты ✅ |
| CRUD в UI | Удаление проектов/серий с подтверждением (каскад) | ✅ |
| Экспорт | Серия → Markdown-файл (скачивание) | ✅ |
| **Синхронизация (v0.3)** | Снапшот-движок: `export_snapshot`/`apply_snapshot` (LWW по `updated_at`), `SyncTransport` (`FileTransport`/`InMemoryTransport`), `sync()` двусторонний; `SystemClock` → секундная точность для корректного строкового LWW; IPC `sync_file` + кнопка «Синхронизация» | ядро **24 теста** (+2 sync), clippy чисто, фронт `vitest` 8/8, `vite build` ✅ |

**Ключевое решение:** синхронизируем не «живой» файл БД, а снапшот с по-сущностным LWW-слиянием — безопасно для нескольких устройств. `FileTransport` в папку Яндекс.Диска даёт рабочий синк уже сейчас; REST/OAuth-адаптер Яндекса — следующий шаг (реализует тот же `SyncTransport`).

**Осталось:** REST/OAuth-адаптер Яндекс.Диска; удаления в синке (сейчас объединение, tombstone'ы — на будущее); версии/вложения/wiki-links; e2e Playwright; иконки бандла.

---

## Как продолжить в новой сессии

1. Подтянуть ветку: `git fetch origin claude/dev-notes-app-design-xa33vz && git checkout claude/dev-notes-app-design-xa33vz`.
2. Прочитать по порядку: [`../CLAUDE.md`](../CLAUDE.md) → [`00-INDEX.md`](00-INDEX.md) → [`02-SPECIFICATION.md`](02-SPECIFICATION.md) → этот WORKLOG (раздел «Что осталось»).
3. Соблюдать конвенции `CLAUDE.md` §3 (документировать файлы, комментарии, кросс-док, тесты §3.7, журнал §3.8).
4. По завершении логического блока — новая запись здесь + строка в журнале `CLAUDE.md` §7 + коммит и push.
